/*const db = require("../database/db");

const getUserProfile = async (req, res) => {
  const query = await db.collection("user").get();
  const users = query.docs.map((user) => user.data());

  const foundUser = users.find((user) => user.token === )

};*/
